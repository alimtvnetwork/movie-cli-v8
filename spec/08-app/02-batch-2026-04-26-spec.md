# Batch Spec — 26-Apr-2026

> **Status**: DRAFT — awaiting user approval before implementation.
> **Author**: AI handoff document. All sections are self-contained.
> **Version target**: bump `version/info.go` from `v2.166.0` → `v2.167.0` after merge.
> **Scope**: deliver plan items 1.2, 2.2, 2.3, 3.2, 3.3, 3.4, 4.1, 4.2, 4.3 + scan/search hardening + multi-source fallback + CI/CD E2E.

---

## 0. Pre-flight findings

Reading the codebase before writing the spec uncovered two things worth flagging:

1. **4.3 `DiscoverByGenre` already exists.** `tmdb/client.go:218` defines `DiscoverByGenre(mediaType, genreID, page)` and it is already used by both `cmd/movie_discover.go:94` and `cmd/movie_suggest_helpers.go:82`. Plan item 4.3 is therefore *implemented*; this batch will only add **acceptance tests + spec entry** to mark it formally DONE.
2. **Search fallback chain already exists.** `tmdb/fallback.go` already implements `SearchWithFallback`: TMDb → progressive title trim → DuckDuckGo → IMDb id → TMDb `/find`. The new "OMDb + IMDb scrape" provider plugs in as a **third tier** between progressive-trim and DuckDuckGo. Existing IMDb cache stays.

---

## 1. Plan items — implementation contracts

### 1.2 Undo confirmation prompt (P0)
- **Files**: `cmd/movie_undo.go`, `cmd/movie_undo_exec.go`
- **Change**: `confirmUndo()` already exists. Audit every undo path (`undoLastOperation`, `undoLastBatch`, `undoActionByID`, `undoMoveByID`) to ensure each one calls `confirmUndo(scanner)` *before* mutating state, and prints the from→to paths before the prompt.
- **Acceptance** (GIVEN/WHEN/THEN):
  - GIVEN a pending move undo WHEN user types `n` THEN no file is moved AND DB is unchanged AND exit code is 0
  - GIVEN `--yes` flag WHEN running undo THEN no prompt is shown AND undo proceeds
  - GIVEN `--id 123` for a delete-action undo WHEN user types `y` THEN snapshot is restored

### 2.2 Shared helper documentation (P1)
- **Files**: `cmd/movie_info.go`, `cmd/movie_resolve.go`, `cmd/movie_search_save.go`, `cmd/movie_scan_helpers.go`
- **Change**: prepend `// SHARED: used by <commands>` to every function that has 2+ callers across `cmd/`. Generate the list with `rg -n "func (\w+)" cmd/movie_info.go cmd/movie_resolve.go` then cross-reference call sites.
- **Acceptance**: every shared helper has a `// SHARED:` comment line directly above its `func` declaration.

### 2.3 `movie ls` filter clarification (P1)
- **Files**: `cmd/movie_ls.go` (top-of-file comment + `runMovieLs` doc), `spec/08-app/01-project-spec.md` §4.6
- **Change**: state explicitly that `ls` returns rows where `original_file_path != ''`. Add an opt-in `--all` flag that includes orphaned (TMDb-only) entries.
- **Acceptance**:
  - GIVEN media with empty `original_file_path` WHEN running `movie ls` THEN it is hidden
  - GIVEN the same WHEN running `movie ls --all` THEN it is shown with a `(no file)` marker

### 3.2 File-size stats (P2)
- **File**: `cmd/movie_stats.go`
- **Change**: add a `FileSizeStats` block printing `Total size`, `Average size`, `Largest file (title + size)`. Use `db.SumFileSize`, `db.AvgFileSize`, `db.LargestMedia` (add to `db/media.go`). Format with `humanize.Bytes`.
- **Acceptance**:
  - GIVEN 3 media rows with sizes 1 GiB, 2 GiB, 3 GiB WHEN running `movie stats` THEN total=6.0 GiB, avg=2.0 GiB, largest title shown
  - GIVEN no rows WHEN running `movie stats` THEN size block is omitted (no divide-by-zero)

### 3.3 Error-handling spec (P1)
- **File**: `spec/08-app/04-error-handling-spec.md` (already exists — extend, don't replace)
- **Add sections**:
  - **TMDb 429 rate limit**: detect `Retry-After` header, sleep + retry once, then surface `apperror.New("TMDb rate limit, try again later")`.
  - **SQLite BUSY (SQLITE_BUSY=5)**: retry up to 3× with 100 ms exponential backoff before failing.
  - **Offline mode**: net.OpError → fall back to local DB only; commands print `⚠ offline — showing cached data`.
- **Acceptance**: spec has 3 new subsections each with: detection signal, retry policy, user-facing message, exit code mapping.

### 3.4 README commands doc (P2)
- **File**: `README.md`
- **Change**: add a `## Commands` table listing all `movie <cmd>` subcommands (currently 25+, not 11 — count was outdated). Each row: name | one-line description | example. Generate the list with `rg -n "Use:\s+\"" cmd/*.go`.
- **Acceptance**: every file in `cmd/movie_*.go` that defines a `cobra.Command` has a row in the table.

### 4.1 Batch move (P3)
- **Files**: `cmd/movie_move.go`, new `cmd/movie_move_all.go`
- **Change**: add `--all` flag. When set, iterate every media row whose `original_file_path` is set, prompt once for the destination root, move each file preserving sub-folder structure, log one `action_history` batch with shared `batch_id`.
- **Acceptance**:
  - GIVEN 5 media files and `movie move --all <dest>` WHEN confirmed THEN all 5 are moved AND a single batch row exists in `action_history`
  - GIVEN `movie undo --batch` after THEN all 5 are reverted

### 4.2 Per-movie JSON metadata sidecar (P2)
- **Folder convention**: `~/.movie-output/metadata/<media_id>.json` (per existing `.movie-output/` spec — **NOT** `.movie/`)
- **Files**: `cmd/movie_scan_process.go`, new helper `cmd/movie_metadata_sidecar.go`
- **Change**: after each successful scan-add, write the full TMDb payload (title, overview, cast, crew, genres, poster URL) as pretty-printed JSON. Atomic write: `tmp` then `os.Rename`.
- **Acceptance**:
  - GIVEN a successful scan of `Inception.mkv` WHEN done THEN `~/.movie-output/metadata/<id>.json` exists with `title="Inception"`
  - GIVEN a sidecar already exists WHEN re-scanning THEN file is overwritten atomically (no partial writes)

### 4.3 DiscoverByGenre (P3) — **already implemented**
- **Action**: add `## 4.3` confirmation note to plan.md and 2 acceptance tests in `tmdb/client_test.go`:
  - GIVEN genreID=28 (Action) WHEN `DiscoverByGenre("movie", 28, 1)` THEN at least 1 result returned
  - GIVEN invalid mediaType="xyz" WHEN called THEN error returned

---

## 2. Scan + search hardening ("must work at all cost")

### 2.1 Scan reliability checklist
- Wrap every TMDb call in `withRetry(3, exponentialBackoff)`.
- Treat zero-byte and `*.part` / `*.crdownload` files as **skip**, not error.
- On any per-file failure: log to `action_history` with `FileAction='scan_error'`, **continue** the loop, increment `failed_count`. Final exit code stays 0 if `succeeded_count > 0`.
- Add `--strict` flag for CI: any failure → non-zero exit.

### 2.2 Search reliability checklist
- New chain order in `tmdb/fallback.go`:
  1. `SearchMulti(title + year)`
  2. `tryProgressiveTrim`
  3. **NEW** `tryOmdb` (if `OMDB_API_KEY` set) — see §3
  4. **NEW** `tryImdbScrape` (no key, last resort) — see §3
  5. existing `tryImdbViaWeb` (DuckDuckGo + cache)
- Every step times out at 10 s; chain total budget 25 s.
- All providers cache into the same `imdb_cache` table.

---

## 3. Multi-source fallback — OMDb + IMDb scrape

### 3.1 OMDb provider (`tmdb/omdb.go` — new)
```go
type OmdbClient struct { APIKey string; HTTP *http.Client }
func (o *OmdbClient) SearchByTitle(title string, year int) ([]SearchResult, error)
func (o *OmdbClient) GetByImdbId(imdbID string) (*SearchResult, error)
```
- Endpoint: `https://www.omdbapi.com/?apikey=KEY&t=TITLE&y=YEAR`
- Map OMDb fields → existing `SearchResult{ID, Title, Year, Overview, MediaType, ImdbID}`. `ID` (TMDb numeric) stays 0 — caller uses `LookupByImdbId` to enrich.
- Config key: `OmdbApiKey` in `config` table; env var `OMDB_API_KEY` as fallback.

### 3.2 IMDb HTML scrape (`tmdb/imdb_scrape.go` — new)
- Endpoint: `https://www.imdb.com/find/?q=TITLE+YEAR&s=tt&ttype=ft`
- Parse with `regexp` (no HTML deps): extract first `tt\d{7,10}` from result list.
- User-Agent: `Mozilla/5.0 (compatible; movie-cli/2.x; +https://github.com/alimtvnetwork/movie-cli-v8)`.
- 10 s timeout. On any error → return nil, log warning, do NOT bubble error.
- Result feeds into existing `lookupByImdbId` for TMDb enrichment.

### 3.3 Wiring
- `tmdb/fallback.go` `SearchWithFallback` becomes a small dispatcher reading a `[]Provider` slice. Each provider implements `Try(title, year string) []SearchResult`.
- This replaces hard-coded ordering and makes adding Trakt/etc. trivial later.

---

## 4. CI/CD end-to-end testing

### 4.1 Go integration tests (`cmd/e2e_test.go` — new, build tag `e2e`)
- Build tag: `//go:build e2e` so they don't run on every `go test`.
- Spin up a fake TMDb HTTP server with `httptest.NewServer` returning canned JSON from `testdata/tmdb/*.json`.
- Use a temp DB (`db.OpenWithPath(t.TempDir() + "/test.db")`).
- Cases:
  1. `movie scan testdata/library/` → adds 3 media, exit 0, prints `Added 3`
  2. `movie search "Inception"` → returns ≥1 row
  3. `movie ls` → 3 rows, `--all` → 3 rows (no orphans seeded)
  4. `movie move <id> <newdir>` → file moved + DB updated
  5. `movie undo --yes` → file restored

### 4.2 Shell smoke (`scripts/e2e-smoke.sh` — new)
```bash
#!/usr/bin/env bash
set -euo pipefail
go build -o /tmp/movie .
export MOVIE_DB="$(mktemp -d)/test.db"
/tmp/movie version | grep -q "v2."
/tmp/movie scan testdata/library --strict
/tmp/movie ls | grep -q "Inception"
/tmp/movie stats | grep -q "Total size"
echo "✅ smoke passed"
```

### 4.3 Test fixtures (`testdata/library/` — new)
- `Inception (2010).mkv` — empty file (0 bytes is OK; scan should skip-with-warn but for fixtures we add 1 byte)
- `The Office S01E01.mkv` — 1 byte
- `Interstellar (2014).mp4` — 1 byte
- `testdata/tmdb/search-inception.json` — canned TMDb response
- `testdata/tmdb/search-office.json` — canned TMDb response

### 4.4 GitHub Actions (`.github/workflows/e2e.yml` — new)
- Trigger: `pull_request` and `push` to `main`
- Steps: setup-go → `go test -tags=e2e ./...` → `bash scripts/e2e-smoke.sh`
- **MUST NOT commit anything back** (per `mem://issues/04-ci-log-commit-loop`).

---

## 5. File-by-file delivery checklist

| # | File | Action | LOC est. |
|---|------|--------|----------|
| 1 | `cmd/movie_undo.go`, `cmd/movie_undo_exec.go` | Edit — confirm prompt audit | +20 |
| 2 | `cmd/movie_info.go`, `cmd/movie_resolve.go`, `cmd/movie_search_save.go` | Edit — `// SHARED:` comments | +15 |
| 3 | `cmd/movie_ls.go` | Edit — `--all` flag + filter doc | +25 |
| 4 | `cmd/movie_stats.go`, `db/media.go` | Edit — file-size aggregates | +60 |
| 5 | `spec/08-app/04-error-handling-spec.md` | Edit — TMDb 429, SQLite BUSY, offline | +80 |
| 6 | `README.md` | Edit — commands table | +90 |
| 7 | `cmd/movie_move_all.go` (new), `cmd/movie_move.go` | Add — `--all` batch flag | +120 |
| 8 | `cmd/movie_metadata_sidecar.go` (new), `cmd/movie_scan_process.go` | Add — JSON sidecar to `~/.movie-output/metadata/` | +90 |
| 9 | `tmdb/client_test.go` | Add — DiscoverByGenre tests | +40 |
| 10 | `tmdb/omdb.go` (new) | Add — OMDb provider | +90 |
| 11 | `tmdb/imdb_scrape.go` (new) | Add — IMDb HTML scrape | +60 |
| 12 | `tmdb/fallback.go` | Edit — provider chain dispatcher | +40 |
| 13 | `cmd/movie_scan_loop.go` | Edit — `--strict`, retry, skip rules | +50 |
| 14 | `cmd/e2e_test.go` (new) | Add — Go integration tests | +200 |
| 15 | `scripts/e2e-smoke.sh` (new) | Add — shell smoke | +25 |
| 16 | `testdata/library/*`, `testdata/tmdb/*.json` (new) | Add — fixtures | n/a |
| 17 | `.github/workflows/e2e.yml` (new) | Add — CI run | +30 |
| 18 | `version/info.go` | Edit — `v2.167.0` | +1 |
| 19 | `plan.md` | Edit — mark 1.2, 2.2, 2.3, 3.2, 3.3, 3.4, 4.1, 4.2, 4.3 ✅ | +20 |
| 20 | `MILESTONES.md` | Append — 26-Apr-2026 entry | +3 |

**Total estimated diff**: ~1,060 lines across 20 files.

---

## 6. Risks & mitigations

| Risk | Mitigation |
|------|------------|
| OMDb daily limit (1k req/day free tier) | Cache hits in `imdb_cache`; provider only invoked on TMDb miss |
| IMDb HTML structure change breaks scraper | Regex matches `tt\d{7,10}` only — robust to layout changes; falls through silently on no match |
| E2E hits real TMDb network in CI | All TMDb calls in `e2e_test.go` use `httptest.NewServer`; smoke script also points at fake via `TMDB_BASE_URL` env |
| Shell smoke fails on Windows runner | Provide `scripts/e2e-smoke.ps1` mirror; CI matrix runs only on `ubuntu-latest` initially |
| `--all` move catastrophe (wrong dest) | Mandatory `[y/N]` prompt showing count + first 3 paths + `--yes` only opt-in |
| Sidecar writes fail mid-scan | Atomic `tmp` + `os.Rename`; failure logged but does **not** abort the scan |
| CI commits back & loops | Workflow has zero `git push` / `git commit` steps; ref `mem://issues/04-ci-log-commit-loop` |

---

## 7. Approval gate

Reply **"approved"** (or with edits) to start implementation. On approval the AI will execute items in this order:

1. Spec (3.3 error-handling) — pure docs, no risk
2. Plan/README docs (2.3 ls clarify, 3.4 README, 2.2 SHARED comments)
3. Code: 1.2 undo audit → 3.2 stats → 4.2 sidecar → 4.1 batch move → 4.3 tests
4. New providers: OMDb → IMDb scrape → fallback dispatcher rewire
5. Scan hardening (`--strict`, retries, skip rules)
6. CI/CD: fixtures → Go e2e tests → shell smoke → workflow YAML
7. Version bump + MILESTONES entry + plan.md status update

After each phase the AI will run `go build ./...` and `go test ./...` (smoke `-tags=e2e` last). Phases are independently revertable.
